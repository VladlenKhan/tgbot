let tg = window.Telegram.WebApp;
let buy = document.getElementById('order');
 
document.getElementById('user_name').value = tg.initDataUnsafe.user.first_name + " " + initDataUnsafe.user.last_name
order.addEventListener('click',()=>{
    tg.close();
})